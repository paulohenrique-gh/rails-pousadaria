<div class="container w-auto">
  <h2 class="text-center mb-3"><%= @room.guesthouse.brand_name %></h2>
  <h3 class="text-center mb-3">Editar Quarto</h3>

  <%= render partial: 'form',
            locals: { resource: room_path(@room.id), method: 'patch' } %>
</div>