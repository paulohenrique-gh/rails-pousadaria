<% if resource.errors.any? %>
  <div>
    <p>O formulário contém os erros abaixo:</p>
    <ul>
      <% resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>